{
  "name": "cnx-cigars-lounge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed for static site' && exit 0"
  },
  "wrangler": {
    "assets": {
      "directory": "."
    }
  }
}